【前提】
手动分出备份空间，大小10GB

【开启保护】
一、【读写】
测试1：
1.用python脚本对保护分区写入大小几KB~几MB的文件，目标总大小是7GB；
2.卸载再挂载保护分区；
3.用python验证文件md5；

【自动合并】
测试1：
1.在【读写】基础上继续写入8GB，发生合并
2.卸载再挂载保护分区；
3.用python验证文件md5；

【关闭保护盘再开启】
二、【恢复】
测试1：
------t1------branch1
         |
         --------------t2------branch2
                             |
                             -------------------branch3

1.用python脚本对保护分区写入10个大小几KB~几MB的文件，记住文件列表，等待30秒，记住当前时间t1，等待2秒；用python脚本对保护分区写入5个大小几KB~几MB的文件；
卸载保护盘
2.将保护盘恢复到t1；挂载保护盘；验证文件列表和md5；
3.用python脚本对保护分区写入10个大小几KB~几MB的文件，记住文件列表，等待30秒，记住当前时间t2，等待2秒；用python脚本对保护分区写入5个大小几KB~几MB的文件；
卸载保护盘
4.将保护盘恢复到t2；挂载保护盘；验证文件列表和md5；
5.用python脚本对保护分区写入10个大小几KB~几MB的文件；

【合并】
测试2：手动合并，验证分支失效
1.执行一次合并，分支1和rr1被删除；分支2和分支3存在，两个分支的rr全部在；
2.验证文件md5；
3.执行一次合并，分支2和rr2被删除；分支3存在，rr全部在；
4.验证文件md5；
5.执行一次合并，无rr被处理；
6.验证文件md5；

【关闭保护盘再开启】
三、【恢复】
测试1：
------t1------t2------branch1
         |           |
         --------------------branch2
                     |       
                     -------------------branch3

1.用python脚本对保护分区写入10个大小几KB~几MB的文件，记住文件列表，等待30秒，记住当前时间t1，等待2秒；用python脚本对保护分区写入10个大小几KB~几MB的文件，记住文件列表，等待30秒，记住当前时间t2，等待2秒；用python脚本对保护分区写入5个大小几KB~几MB的文件；
卸载保护盘
2.将保护盘恢复到t1；挂载保护盘；验证文件列表和md5；用python脚本对保护分区写入5个大小几KB~几MB的文件；
卸载保护盘
3.将保护盘恢复到t2；挂载保护盘；验证文件列表和md5；用python脚本对保护分区写入5个大小几KB~几MB的文件；

【合并】
测试3：手动合并，验证分支失效
1.执行一次合并，分支1和rr1被删除；分支2和rr2被删除；分支3存在，rr全部在；
2.验证文件md5；

【关闭保护盘再开启】
四、【恢复】
测试1：
------t1------------branch1
         |           
         --------------------branch2
         |       
         --------------------branch3

1.用python脚本对保护分区写入10个大小几KB~几MB的文件，记住文件列表，等待30秒，记住当前时间t1，等待2秒；用python脚本对保护分区写入10个大小几KB~几MB的文件；
卸载保护盘
2.将保护盘恢复到t1；挂载保护盘；验证文件列表和md5；用python脚本对保护分区写入5个大小几KB~几MB的文件；
卸载保护盘
3.将保护盘恢复到t1；挂载保护盘；验证文件列表和md5；用python脚本对保护分区写入5个大小几KB~几MB的文件；

【合并】
测试4：手动合并，验证分支失效
1.执行一次合并，分支1和rr1被删除；分支2和分支3存在，rr全部在；
2.验证文件列表和md5；
3.执行一次合并，分支2被删除，分支3存在，rr全部在；
4.验证文件列表和md5；
5.执行一次合并，没rr被处理，rr全部在；
6.验证文件列表和md5；

【关闭保护盘再开启】
五、【关闭保护】
1.用python脚本对保护分区写入大小几KB~几MB的文件，目标总大小是2GB；
2.用python脚本对保护分区写入大小几KB~几MB的文件，目标总大小是2GB，同时关闭保护；
3.验证文件md5；

【关闭保护盘再开启】
六、【还原点】
测试1：
0.用python脚本对保护分区写入10个大小几KB~几MB的文件，等待30秒，记住当前时间t1，等待2秒；设置t1为还原点；
1.用python脚本对保护分区写入大小几KB~几MB的文件，目标总大小是1GB，删除所有文件，重复执行15次；
2.触发自动合并；
3.验证文件md5；